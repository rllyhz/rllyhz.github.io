import {
  createHorizontalSpacer,
  createVerticalSpacer,
  createWelcomeUI,
  createTitleApp,
} from "../../utils/ui/component-helpers";

const Component = {
  createHorizontalSpacer,
  createVerticalSpacer,
  createWelcomeUI,
  createTitleApp,
};

export default Component;
