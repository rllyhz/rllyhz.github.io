import { css } from "../../core/Style";

const styles = /* css */`
  .container {
    display: block;
    padding: .85rem;
    background-color: var(--loading-color);
    border-radius: 8px;
  }
`;

export default css(styles);
